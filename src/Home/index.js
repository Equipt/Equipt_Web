import React from 'react';
import theme from '../theme.js';

export default () => (
  <section style={ styles.container }>
    Home
  </section>
)

const styles = {
  container: {
    ...theme.container
  }
}
